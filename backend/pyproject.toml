[project]
name = "review-reply-bot-backend"
version = "0.1.0"
description = "FastAPI backend for Review-Reply Bot (SQLAlchemy store, Milestone 4)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "python-multipart>=0.0.18",
  "bcrypt>=4.1",
  "pyjwt>=2.8",
  "openai>=3.8.0",
  "pydantic-settings>=2.15.0",
  "sqlalchemy>=2.0.52",
]

[dependency-groups]
dev = [
  "pytest>=8",
  "httpx>=0.27",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
